Android application that takes voice input or text input from camera in English and translates the same to Hindi.

Uses Google spreadsheet API for translation with a ruby server

Uses Google's servers to generate hindi speech
